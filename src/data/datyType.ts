export type Clothing = {
  id?: string;
  name: string;
  price: number;
  disponible: boolean;
  category: string;
  imageUrl: string;
};

export type Category = {
  id?: string;
  name: string;
};